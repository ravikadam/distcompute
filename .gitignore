node_modules/
dist/
.DS_Store
*.log
.system_generated/
# Local runtime state (may contain a wandb API key) and event logs
config.json
logs/
# Saved training runs (weights + Adam snapshots; can be large)
checkpoints/
# Throwaway integration-test scratch scripts
_test_*.js
_diag*.js
_*_result.txt

# Local training datasets
data/
# Python bytecode (from the exported-weights inference tool)
__pycache__/
*.pyc
# PR scratch (not part of the repo)
PULL_REQUEST*.md
